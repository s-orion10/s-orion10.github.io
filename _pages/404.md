---
title: "Page Not Found"
sitemap: false
permalink: /404.html
---

<style>
:root{
  --cs-bg: radial-gradient(circle at top, #e0f7ff 0%, #f8fafc 55%, #e2e8f0 100%);
  --cs-card-bg: rgba(255, 255, 255, 0.92);
  --cs-card-border: rgba(148, 163, 184, 0.22);
  --cs-card-shadow: 0 20px 45px rgba(15, 23, 42, 0.18);
  --cs-title: #0f172a;
  --cs-note: #475569;

  --cs-btn-bg: linear-gradient(135deg, #10b981, #14b8a6);
  --cs-btn-color: #ffffff;
  --cs-btn-shadow: 0 15px 30px rgba(20, 184, 166, 0.38);
  --cs-btn-hover-shadow: 0 24px 40px rgba(20, 184, 166, 0.45);
  --cs-focus: rgba(45, 212, 191, 0.45);
}

html[data-theme="dark"]{
  --cs-bg: radial-gradient(circle at top, rgba(45, 212, 191, 0.12) 0%, rgba(15, 23, 42, 0.92) 55%, rgba(2, 6, 23, 0.96) 100%);
  --cs-card-bg: rgba(52, 63, 72, 0.92);
  --cs-card-border: rgba(191, 219, 254, 0.22);
  --cs-card-shadow: 0 18px 44px rgba(0, 0, 0, 0.45);
  --cs-title: #f1f5f9;
  --cs-note: rgba(226, 232, 240, 0.78);

  --cs-btn-bg: linear-gradient(135deg, rgba(16, 185, 129, 0.95), rgba(20, 184, 166, 0.95));
  --cs-btn-color: #0b1220;
  --cs-btn-shadow: 0 14px 28px rgba(0, 0, 0, 0.35);
  --cs-btn-hover-shadow: 0 22px 40px rgba(0, 0, 0, 0.45);
  --cs-focus: rgba(94, 234, 212, 0.45);
}

.coming-soon-wrapper{
  display:flex;
  align-items:center;
  justify-content:center;
  min-height:60vh;
  background: var(--cs-bg);
  padding: 3rem 1.5rem;
}

.coming-soon-card{
  max-width: 520px;
  text-align:center;
  padding: 2.5rem 2rem;
  border-radius: 24px;
  background: var(--cs-card-bg);
  box-shadow: var(--cs-card-shadow);
  border: 1px solid var(--cs-card-border);
  backdrop-filter: blur(6px);
}

.coming-soon-title{
  font-size: 2rem;
  font-weight: 750;
  color: var(--cs-title);
  margin: 0 0 1.6rem;
  letter-spacing: 0.01em;
}

.coming-soon-actions{
  display:flex;
  flex-direction:column;
  gap: 0.85rem;
  align-items:center;
}

.coming-soon-home{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap: 0.5rem;
  padding: 0.75rem 1.4rem;
  border-radius: 999px;
  font-weight: 650;
  background: var(--cs-btn-bg);
  color: var(--cs-btn-color);
  text-decoration:none;
  box-shadow: var(--cs-btn-shadow);
  transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease;
}

.coming-soon-home:hover{
  transform: translateY(-2px);
  box-shadow: var(--cs-btn-hover-shadow);
  filter: brightness(1.02);
}

.coming-soon-home:focus-visible{
  outline: 3px solid var(--cs-focus);
  outline-offset: 3px;
}

.coming-soon-note{
  font-size: 0.95rem;
  color: var(--cs-note);
  margin: 0;
  line-height: 1.55;
}

@media (max-width: 600px){
  .coming-soon-card{ padding: 2.2rem 1.6rem; }
  .coming-soon-title{ font-size: 1.7rem; }
}
</style>

<div class="coming-soon-wrapper">
  <div class="coming-soon-card">
    <p class="coming-soon-title">Coming Soon</p>
    <div class="coming-soon-actions">
      <a class="coming-soon-home" href="{{ '/' | relative_url }}">Back to Home</a>
      <p class="coming-soon-note">If you think this is a mistake, feel free to contact me and I’ll fix it as soon as possible.</p>
    </div>
  </div>
</div>
